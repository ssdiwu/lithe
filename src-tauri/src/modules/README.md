# Native subsystems

Each child module owns one operating-system boundary or native subsystem. Keep
Tauri command inputs validated here and expose only the commands registered by
the application runtime.
