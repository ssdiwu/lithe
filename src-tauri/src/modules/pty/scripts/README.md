# Shell integration scripts

These scripts add OSC 7 and OSC 133 reporting to shells started by Lithe. They
must use the `LITHE_*` environment namespace and must not modify a user's shell
configuration files in place.
