# Native LSP sessions

This module resolves language-server binaries and environments, frames LSP
messages, tracks resource usage, and owns native server session lifecycles.
Frontend protocol handling remains under `src/modules/lsp/`.
