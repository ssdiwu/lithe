# Rust test support

Shared integration-test fixtures and command helpers live here. They create
isolated temporary repositories and normalize platform differences for tests
under `src-tauri/tests/`.
