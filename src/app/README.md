# Main application composition

This directory composes the main Lithe window from feature modules. Keep global
providers and application-wide dialogs at this layer; feature behavior belongs
in its owning module.
