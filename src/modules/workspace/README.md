# Workspace environment

This module represents where commands and project operations run. It exposes
the local environment on every platform and named WSL distributions on Windows,
plus the persisted current environment store used by spaces and terminals.
