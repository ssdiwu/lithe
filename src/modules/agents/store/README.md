# Agent notification stores

These stores combine status from Lithe's built-in AI runtime and supported
terminal agents, and retain notification state used by the header and toast
surfaces. They do not own provider conversations or AI tool execution.
