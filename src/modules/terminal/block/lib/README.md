# Terminal block helpers

This directory contains the editor, history, completion, and presentation helpers used by the block-style terminal input.

User-visible copy belongs in `src/i18n/locales/` and is passed into these helpers by their React callers.
