# Inline autocomplete

This directory owns CodeMirror's inline AI completion extension, prompt
construction, indentation normalization, suggestion trimming, and provider
dispatch. Provider credentials still flow through the shared AI keyring layer.
