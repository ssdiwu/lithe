# Bundled themes

Each file defines one built-in Lithe theme. `index.ts` is the registry consumed
by the theme system, and `lithe-default.ts` owns the product default. Persisted
IDs must stay stable so existing user selections keep working.
