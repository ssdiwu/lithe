# Theme system

This module owns bundled and custom themes, background images, theme file import
and export, and the application-wide theme provider. New persisted identifiers
and exported files use the Lithe namespace.
