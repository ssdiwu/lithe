# Built-in AI tools

Tool definitions exposed to Lithe's built-in AI runtime live here. They bridge
the model to contextual reads, search, reviewed edits, shell and terminal
operations, todos, and sub-agents.

Keep workspace authorization, approval requirements, path validation, and
network boundaries intact when changing a tool. Terminal agents such as Pi do
not execute through this directory.
