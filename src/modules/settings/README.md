# Settings persistence

This module owns persisted application preferences and their cross-window event
bridge. Persist new Lithe settings under the `lithe` namespace so installations
do not read or overwrite Terax user data.
